import * as React from "react";

import { TagCount } from "../interfaces";

interface Props {
  tags: TagCount;
}

/**
 * Tags component.
 * @returns {React.ReactElement<Props>}
 *    the react node that represents the tags.
 *
 * @public
 * @function
 */
export default function Tags({ tags }: Props): React.ReactElement<Props> {
  return null;
}
